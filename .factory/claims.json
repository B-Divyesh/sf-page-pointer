[
  {
    "id": "demo-sandbox",
    "claim": "Try it with sample data opens a working sample guide without using real reading data.",
    "where": "landing action and /demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "fresh browser context opens /demo, moves the guide, records a sample session, and asserts only demo:page-pointer exists"
  },
  {
    "id": "demo-reset",
    "claim": "Demo data can be reset or discarded before starting for real.",
    "where": "/demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-reset",
    "sandbox": "fresh /demo context records a sample session, resets it, verifies an empty demo store, then starts real mode"
  },
  {
    "id": "local-only-reading",
    "claim": "Frames stay on this device and the exported reading data contains no page image or recognized text.",
    "where": "landing facts and privacy section",
    "test": "npm run test:e2e -- --grep @claim:local-only-reading",
    "sandbox": "fresh /demo context records every request and inspects the exported JSON"
  },
  {
    "id": "offline-demo",
    "claim": "The sample guide works offline after its first visit.",
    "where": "landing facts and README",
    "test": "npm run test:e2e -- --grep @claim:offline-demo",
    "sandbox": "fresh /demo context waits for the service worker, goes offline, reloads, and moves the guide"
  },
  {
    "id": "free-guide-and-price",
    "claim": "The core guide needs no account; the optional Supporter pack costs ₹249 once.",
    "where": "landing facts and Supporter section",
    "test": "npm run test:e2e -- --grep @claim:free-guide-and-price",
    "sandbox": "fresh browser context checks the price and opens the working sample guide without sign-in"
  }
]
