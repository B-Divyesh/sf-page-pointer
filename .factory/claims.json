[
  {
    "id": "demo-sandbox",
    "claim": "The sample guide uses separate demo data and supports tap, word or line mode, Next, Previous, Arrow keys, and Space.",
    "where": "landing demo action, guide controls, README",
    "test": "npm run test:e2e -- --grep @claim:demo-sandbox",
    "sandbox": "fresh /demo context places and moves the sample guide with every public control, records a session, and asserts only demo:page-pointer exists"
  },
  {
    "id": "demo-reset",
    "claim": "Demo data can be reset or discarded before starting for real.",
    "where": "/demo banner and README",
    "test": "npm run test:e2e -- --grep @claim:demo-reset",
    "sandbox": "fresh /demo context records a sample session, resets it, verifies an empty demo store, then starts real mode"
  },
  {
    "id": "local-ink-detection",
    "claim": "Page Pointer groups local contrasting pixels into printed lines and word-like boxes without reading the text.",
    "where": "How it works, privacy section, README",
    "test": "npm test -- --testNamePattern @claim:local-ink-detection",
    "sandbox": "synthetic two-line pixel fixture is processed in memory and produces multiple word boxes without any browser or network API"
  },
  {
    "id": "camera-states",
    "claim": "The guide requests the rear camera without sending frames, saves a brief summary, releases the stream, and explains recovery states.",
    "where": "Open camera flow, privacy copy, error states, README",
    "test": "npm run test:e2e -- --grep @claim:camera-states",
    "sandbox": "fresh demo-to-real context records requests and a camera summary, then uses a blank frame, denied permission fixture, and missing-camera fixture"
  },
  {
    "id": "local-only-reading",
    "claim": "Reading sends no third-party request, and exported data contains no page image or recognized text.",
    "where": "landing facts, privacy section, Privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:local-only-reading",
    "sandbox": "fresh /demo context records every request and inspects the complete exported JSON"
  },
  {
    "id": "local-data-roundtrip",
    "claim": "Users can import, export, and erase local preferences and up to 50 brief session summaries.",
    "where": "Local data settings, Privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:local-data-roundtrip",
    "sandbox": "fresh demo-to-real context imports 54 fixture sessions, exports exactly 50 with preferences, confirms erasure, and exports an empty store"
  },
  {
    "id": "offline-demo",
    "claim": "The camera guide and sample guide work offline after the first visit.",
    "where": "landing facts, network state, README",
    "test": "npm run test:e2e -- --grep @claim:offline-demo",
    "sandbox": "new isolated browser context visits once, goes offline, reloads the real guide with a fake rear camera, exercises its controls, then opens and moves the sample guide"
  },
  {
    "id": "pwa-update",
    "claim": "The installable app precaches its shell and offers an Update action when a new worker is waiting.",
    "where": "manifest, update notice, README",
    "test": "npm run test:e2e -- --grep @claim:pwa-update",
    "sandbox": "fresh /demo context on a local two-generation server validates the manifest and cache, installs a changed worker, uses Update, and confirms activation"
  },
  {
    "id": "free-core",
    "claim": "Parents and tutors can use the complete reading guide without an account.",
    "where": "landing facts, Supporter section, Terms, README",
    "test": "npm run test:e2e -- --grep @claim:free-core",
    "sandbox": "fresh unauthenticated real context opens the fake rear camera, places and moves the guide, changes mode, and confirms no sign-in or license gate"
  },
  {
    "id": "paid-supporter",
    "claim": "The optional Supporter pack costs ₹249 once and adds saved guide colors plus a quiet ten-minute timer.",
    "where": "Supporter section, Terms, README",
    "test": "npm run test:e2e -- --grep @claim:paid-supporter",
    "sandbox": "fresh real context asserts a recorded INR 249 one-time catalogue and checkout fixture, then checks purchase, restore, locking, saved color, timer completion, and the exact checkout URL"
  },
  {
    "id": "license-cache-24h",
    "claim": "Automatic license checks reuse a result for 24 hours.",
    "where": "README and billing documentation",
    "test": "npm run test:e2e -- --grep @claim:license-cache-24h",
    "sandbox": "controlled browser clock verifies one check at restore, no new check before 24 hours, and a new check after 24 hours"
  },
  {
    "id": "private-runtime",
    "claim": "The app loads no tracking, third-party code, remote fonts, or embedded checkout.",
    "where": "Privacy page and README",
    "test": "npm run test:e2e -- --grep @claim:private-runtime",
    "sandbox": "fresh demo flow records every request and inspects loaded scripts, fonts, frames, and storage for outside runtime content"
  }
]
